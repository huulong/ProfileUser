var typed = new Typed(".multiple-text", {
    strings:["Mobile Developer","............","Mobile Developer"],
    typeSpeed: 100,
    backSpeed:100,
    backDelay:1000,
    loop:true//(lặp lại) được thiết lập giá trị là true,
    // cho phép chuỗi các ký tự sẽ được lặp lại liên tục.
})